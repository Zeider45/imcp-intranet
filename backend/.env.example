# Django Settings
SECRET_KEY=django-insecure-change-this-in-production
DEBUG=True
# Hosts allowed to serve the Django app
ALLOWED_HOSTS=localhost,127.0.0.1,imcp-intranet.local,172.16.101.106

# Database Configuration
# SQLite is used by default. For PostgreSQL or MySQL, configure accordingly:
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# CORS Settings (Frontend URLs)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://imcp-intranet.local

# Active Directory / LDAP Configuration with django-auth-ldap
# Example configuration for imcp-intranet.local domain
# 
# For "Strong(er) authentication required" error, use one of these options:
# 1. Use ldap:// with START_TLS (automatically enabled in settings.py) - port 389
# 2. Use ldaps:// for secure LDAP (recommended for production) - port 636
#
# LDAPS example (SSL/TLS on port 636):
#   AUTH_LDAP_SERVER_URI=ldaps://172.16.101.106:636
# Plain LDAP with START_TLS example (port 389):
#   AUTH_LDAP_SERVER_URI=ldap://172.16.101.106:389
#
# Note: For LDAPS with self-signed certificates, the system is configured to accept them.
# For production with valid certificates, update settings.py accordingly.
#
AUTH_LDAP_SERVER_URI=ldap://172.16.101.106:389
AUTH_LDAP_BIND_DN=CN=administrator,CN=Users,DC=imcp-intranet,DC=local
AUTH_LDAP_BIND_PASSWORD=your_password_here
AUTH_LDAP_USER_SEARCH_BASE=DC=imcp-intranet,DC=local
# Filter for user authentication - %(user)s is replaced with the username during login
AUTH_LDAP_USER_SEARCH_FILTER=(sAMAccountName=%(user)s)
# Optional: Network timeout in seconds for LDAP connections (default: 5)
# Increase this value if you have slow network or the LDAP server is far away
# Decrease to fail faster when LDAP server is unreachable (minimum: 3)
# AUTH_LDAP_NETWORK_TIMEOUT=5
# Optional: Filter for sync_ldap_users command to find all users (if not set, uses a sensible default)
# AUTH_LDAP_SYNC_FILTER=(&(objectClass=user)(sAMAccountName=*)(!(objectClass=computer)))
